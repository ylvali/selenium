language: node_js

node_js:
    - "8"
    - "10"

sudo: required
dist: trusty
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
  
git:
    submodules: false

before_script:
    - npm install

script:
    - npm test

