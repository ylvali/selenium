language: node_js

node_js:
    - "8"
    - "10"

sudo: required

before_install:
  - export PATH=$PATH:$PWD/geckodriver

addons:
  chrome: stable
  
git:
    submodules: false

before_script:
    - npm install

script:
    - npm test

